let repsonse = window.prompt("Do you head left or right?");
console.log(repsonse);
if(repsonse === 'left'){
    //do something to coordinate with left//
    console.log('do something to coordinate with left')
} if else(repsonse === 'right'){
    //do something to coordinate with right//
    console.log('do something to coordinate with right');
} else(){
    //user ignored the the wishes of the council//
    console.log('user ignored the the wishes of the council');
}